# Core dependencies
fastapi>=0.115.0
pydantic>=2.0.0
httpx>=0.27.0
uvicorn>=0.30.0

# Testing dependencies
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Security scanning tools
charset-normalizer>=3.0.0

# Database (for scan storage)
psycopg[binary]>=3.2.0

# Static analysis tools (stage2 - SAST)
bandit>=1.8.0

# Cisco AI Defense Skill Scanner (stage3 - behavioral analysis)
skill-scanner>=0.2.0

# Snyk Agent Scan (stage3 - optional cloud-dependent scanner)
# Invoked via: uvx snyk-agent-scan@latest --skills <dir> --json --opt-out
# Note: Sends data to Snyk's cloud - used for corroboration only

# Secrets detection (stage4)
detect-secrets>=1.5.0

# Dependency auditing (stage5)
pip-audit>=2.7.0

# SARIF output support (required by skill-scanner)
sarif-om>=1.0.0
jschema-to-python>=1.2.0
