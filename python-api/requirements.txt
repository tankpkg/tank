# Core dependencies
fastapi>=0.115.0
pydantic>=2.0.0
httpx>=0.27.0

# Testing dependencies
pytest>=8.0.0

# Security scanning tools
charset-normalizer>=3.0.0

# Database (for scan storage)
psycopg[binary]>=3.2.0

# Static analysis tools (optional - for stage2)
# semgrep>=1.50.0
# bandit>=1.7.0

# Secrets detection (optional - for stage4)
# detect-secrets>=1.4.0

# Dependency auditing (optional - for stage5)
# pip-audit>=2.6.0
