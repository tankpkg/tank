apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tank.fullname" . }}-scanner
  labels:
    {{- include "tank.scanner.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.scanner.replicaCount }}
  selector:
    matchLabels:
      {{- include "tank.scanner.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/shared-config: {{ include (print $.Template.BasePath "/shared/configmap.yaml") . | sha256sum }}
        checksum/scanner-config: {{ include (print $.Template.BasePath "/scanner/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/shared/secret.yaml") . | sha256sum }}
        {{- with .Values.scanner.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "tank.scanner.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.scanner.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "tank.serviceAccountName" . }}
      {{- with .Values.scanner.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z {{ include "tank.postgresHost" . }} {{ .Values.global.postgresql.port }}; do
                echo "Waiting for PostgreSQL..."
                sleep 2
              done
              echo "PostgreSQL is ready"
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 32Mi
      containers:
        - name: scanner
          {{- with .Values.scanner.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.scanner.image.repository }}:{{ .Values.scanner.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.scanner.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "tank.sharedConfigmapName" . }}
            - configMapRef:
                name: {{ include "tank.scannerConfigmapName" . }}
            - secretRef:
                name: {{ include "tank.secretName" . }}
          {{- with .Values.scanner.startupProbe }}
          startupProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.scanner.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.scanner.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.scanner.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.scanner.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.scanner.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.scanner.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
