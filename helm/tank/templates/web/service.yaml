apiVersion: v1
kind: Service
metadata:
  name: {{ include "tank.fullname" . }}-web
  labels:
    {{- include "tank.web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tank.web.selectorLabels" . | nindent 4 }}
