# Tank Documentation (llms-full)

> Full markdown export for offline/LLM ingestion. Canonical docs remain under `/docs`.

## Table of Contents
- [Tank Documentation](https://tankpkg.dev/docs)
- [Getting Started](https://tankpkg.dev/docs/getting-started)
- [Publishing Skills](https://tankpkg.dev/docs/publishing)
- [Installing Skills](https://tankpkg.dev/docs/installing)
- [CLI Reference](https://tankpkg.dev/docs/cli)
- [API Reference](https://tankpkg.dev/docs/api)
- [Self-Hosting](https://tankpkg.dev/docs/self-hosting)

---

## Tank Documentation
Canonical URL: https://tankpkg.dev/docs
Description: Start here for setup, publishing, installation, API, and self-hosting.

# Tank Documentation

Tank is a security-first package manager for AI agent skills.

Use this docs set by intent:

- **Learn fast**: [Getting Started](/docs/getting-started)
- **Ship a skill**: [Publishing](/docs/publishing)
- **Use skills in agents**: [Installing](/docs/installing)
- **Command lookup**: [CLI Reference](/docs/cli)
- **Integration lookup**: [API Reference](/docs/api)
- **Run your own registry**: [Self-Hosting](/docs/self-hosting)

## Product Guarantees

- SHA-512 integrity verification on install
- Mandatory security scanning before publish
- Permission declarations in `skills.json`
- Deterministic lockfile workflow

## Choose Your Path

### I am a skill publisher

1. Follow [Getting Started](/docs/getting-started)
2. Implement your manifest and code via [Publishing](/docs/publishing)
3. Validate with `tank doctor` and `tank publish --dry-run`
4. Publish and verify via `tank info @org/name`

### I am a skill consumer

1. Install a package with [Installing](/docs/installing)
2. Review permissions with `tank permissions`
3. Verify integrity with `tank verify`

### I need implementation details

- Commands: [CLI Reference](/docs/cli)
- Endpoints: [API Reference](/docs/api)
- Ops: [Self-Hosting](/docs/self-hosting)

## Canonical References

- CLI entrypoint: `apps/cli/src/bin/tank.ts`
- API routes: `apps/web/app/api/v1/**`
- Security scanner: `python-api/lib/scan/**`

---

## Getting Started
Canonical URL: https://tankpkg.dev/docs/getting-started
Description: Install Tank, authenticate, and verify your first working flow.

# Getting Started

This guide gets you from zero to a verified working setup.

## Prerequisites

- Node.js 24+
- pnpm 10+
- GitHub account (for login)

## 1) Install the CLI

```bash
pnpm add -g @tank/cli
# or: npm i -g @tank/cli
```

Verify:

```bash
tank --version
```

## 2) Authenticate

```bash
tank login
```

This opens browser auth and stores your token in `~/.tank/config.json`.

Check identity:

```bash
tank whoami
```

## 3) Install a skill

```bash
tank install @org/skill-name
```

Optional global install:

```bash
tank install @org/skill-name '*' -g
```

## 4) Verify safety and integrity

```bash
tank permissions
tank verify
tank audit @org/skill-name
```

## 5) Create your first skill (publisher path)

```bash
mkdir my-skill && cd my-skill
tank init
```

Then publish safely:

```bash
tank doctor
tank publish --dry-run
tank publish
```

## Success Criteria

You are done when all are true:

- `tank whoami` shows your user
- `tank install` succeeds
- `tank verify` succeeds
- `tank permissions` shows expected access

## Troubleshooting

### Login works but commands fail

Run:

```bash
tank whoami
tank doctor
```

### Install fails on integrity

Re-run install and verify network/proxy stability. Do not bypass verification.

### Permission output looks too broad

Prefer packages with narrow, explicit permission scopes.

---

## Publishing Skills
Canonical URL: https://tankpkg.dev/docs/publishing
Description: Build, validate, and publish skills with accurate metadata and minimal permissions.

# Publishing Skills

This is a task-focused guide for shipping production-grade skills.

## Preflight Checklist

Before publishing, ensure:

- `skills.json` exists and is valid
- Version is correct (SemVer)
- Permissions are minimal
- README and usage examples exist
- `tank login` already completed

## skills.json (minimal example)

```json
{
  "name": "my-skill",
  "version": "1.0.0",
  "description": "Does one thing well",
  "permissions": {
    "network": { "outbound": ["api.openai.com"] },
    "filesystem": { "read": ["./data/**"], "write": ["./output/**"] },
    "subprocess": false,
    "environment": ["OPENAI_API_KEY"]
  }
}
```

## Publish Flow

```bash
tank doctor
tank publish --dry-run
tank publish
```

Supported publish flags:

- `--dry-run`
- `--private`
- `--visibility <mode>`

## Versioning Policy

- **Patch**: bug fixes
- **Minor**: backward-compatible features
- **Major**: breaking changes

Update version before each release.

## Post-publish Validation

```bash
tank info @org/my-skill
tank audit @org/my-skill
```

Confirm:

- package appears in registry
- permissions are accurate
- no unexpected high/critical findings

## Security Publishing Rules

- Request only permissions your code actually needs
- Avoid broad wildcards for network/filesystem
- Never embed secrets in source
- Keep dependencies pinned and reviewed

## Common Failures

### Validation failure

Fix `skills.json` shape and rerun `tank publish --dry-run`.

### Security finding blocks release

Address the finding; do not bypass scanner gates.

### Wrong visibility

Republish with explicit `--private` or `--visibility`.

---

## Installing Skills
Canonical URL: https://tankpkg.dev/docs/installing
Description: Install, update, remove, and verify skills safely.

# Installing Skills

This guide covers day-2 operations for skill consumers.

## Install

```bash
tank install @org/skill-name
tank install @org/skill-name '^1.2.0'
```

Optional global mode:

```bash
tank install @org/skill-name '*' -g
```

## Update

```bash
tank update
tank update @org/skill-name
```

Optional global mode:

```bash
tank update @org/skill-name -g
```

## Remove

```bash
tank remove @org/skill-name
tank remove @org/skill-name -g
```

## Verify and Audit

```bash
tank verify
tank permissions
tank audit
tank audit @org/skill-name
```

## Discover and Inspect

```bash
tank search "embedding"
tank info @org/skill-name
```

## Operational Guidance

- Run `tank verify` after changes to lock/install state
- Run `tank permissions` before enabling skills in production agents
- Prefer pinned versions for production stability

## Failure Handling

### Install fails

Run `tank doctor`; check auth and network.

### Unexpected permission scope

Remove package and select an alternative with narrower access.

### Verify fails

Reinstall package and ensure lock state is consistent.

---

## CLI Reference
Canonical URL: https://tankpkg.dev/docs/cli
Description: Authoritative command reference from the current CLI entrypoint.

# CLI Reference

Source of truth: `apps/cli/src/bin/tank.ts`.

## Commands

| Command | Args | Options | Purpose |
|---|---|---|---|
| `tank init` | - | - | Initialize `skills.json` |
| `tank login` | - | - | Authenticate via browser |
| `tank whoami` | - | - | Show current user |
| `tank logout` | - | - | Clear auth token |
| `tank publish` | - | `--dry-run`, `--private`, `--visibility <mode>` | Publish a skill |
| `tank install` | `[name] [version-range]` | `-g, --global` | Install one/all skills |
| `tank remove` | `<name>` | `-g, --global` | Remove installed skill |
| `tank update` | `[name]` | `-g, --global` | Update one/all skills |
| `tank verify` | - | - | Verify installation integrity |
| `tank permissions` | - | - | Show resolved permissions |
| `tank search` | `<query>` | - | Search registry |
| `tank info` | `<name>` | - | Show package details |
| `tank audit` | `[name]` | - | Run/read security audit |
| `tank link` | - | - | Link local skills for agents |
| `tank unlink` | - | - | Remove links |
| `tank doctor` | - | - | Diagnose environment/setup |

## Typical Workflows

### Consumer

```bash
tank login
tank install @org/skill-name
tank permissions
tank verify
```

### Publisher

```bash
tank login
tank init
tank publish --dry-run
tank publish
```

### Maintenance

```bash
tank update
tank audit
tank doctor
```

## Notes

- Use `tank <command> --help` for runtime help text.
- Commands not listed above are not part of current CLI entrypoint.

---

## API Reference
Canonical URL: https://tankpkg.dev/docs/api
Description: Public API endpoints implemented under /api/v1.

# API Reference

Base URL (hosted):

```text
https://tankpkg.dev/api/v1
```

Authentication for protected endpoints uses Bearer API keys.

## CLI Auth Endpoints

| Endpoint | Method | Purpose |
|---|---|---|
| `/cli-auth/start` | POST | Start browser-auth flow |
| `/cli-auth/authorize` | GET | User authorization step |
| `/cli-auth/exchange` | POST | Exchange poll token for API key |

## Registry Endpoints

| Endpoint | Method | Purpose |
|---|---|---|
| `/skills` | GET | Search/list skills |
| `/skills` | POST | Start publish flow |
| `/skills/confirm` | POST | Confirm finalize publish |
| `/skills/[name]` | GET | Skill metadata |
| `/skills/[name]/versions` | GET | List versions |
| `/skills/[name]/star` | POST/DELETE | Star/unstar |
| `/skills/[name]/[version]` | GET | Version metadata |
| `/skills/[name]/[version]/files/[...path]` | GET | File content retrieval |
| `/search` | GET | Search endpoint |

## Minimal Usage Example

```bash
curl -H "Authorization: Bearer $TANK_API_KEY"   https://tankpkg.dev/api/v1/skills/my-org/my-skill
```

## Stability Guidance

- Treat endpoint paths above as canonical.
- For payload details, validate against current route handlers in `apps/web/app/api/v1/**`.
- Keep clients resilient to additive response fields.

---

## Self-Hosting
Canonical URL: https://tankpkg.dev/docs/self-hosting
Description: Repository-aligned runbook for self-hosting Tank.

# Self-Hosting

This runbook is aligned to the current monorepo layout.

## Architecture

- **Web app**: `apps/web` (Next.js)
- **Scanner**: `python-api` (FastAPI pipeline)
- **Shared contracts**: `packages/shared`
- **Storage**: Supabase or compatible object storage
- **Database**: PostgreSQL

## Prerequisites

- Node.js 24+
- pnpm 10+
- Python 3.14+
- PostgreSQL instance
- Storage credentials

## Minimum Setup

```bash
pnpm install
cp .env.example .env.local
```

Required envs (minimum):

- `DATABASE_URL`
- `BETTER_AUTH_SECRET`
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `PYTHON_API_URL`

## Build and Run

```bash
pnpm turbo build --filter=@tank/web...
pnpm --filter=@tank/web start
```

## Schema / Migrations

```bash
pnpm --filter=web drizzle-kit generate
pnpm --filter=web drizzle-kit push
```

## Admin Bootstrap

```bash
pnpm --filter=web admin:bootstrap
```

## Health and Verification

- Web health endpoint: `/api/health`
- Validate publish/install flows with CLI in a clean environment
- Confirm scanner connectivity via `PYTHON_API_URL`

## Operational Notes

- Keep `python-api/` and `apps/web/api-python/` in sync when scanner logic changes.
- Use Turbo builds for dependency-aware monorepo builds.
- Do not bypass auth secret requirements in production.

## Troubleshooting

### Build fails with workspace package resolution

Use monorepo-aware build command:

```bash
pnpm turbo build --filter=@tank/web...
```

### Auth runtime errors

Set `BETTER_AUTH_SECRET` explicitly in deployment environment.

### Scanner errors

Verify `PYTHON_API_URL` and scanner deployment health.
