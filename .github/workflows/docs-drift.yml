name: Docs Drift Check

on:
  pull_request:
    paths:
      - "packages/cli/src/**/*.ts"
      - "apps/registry/src/api/**/*.ts"
      - "apps/registry/public/docs/**"
  push:
    branches: [main]
    paths:
      - "packages/cli/src/**/*.ts"
      - "apps/registry/src/api/**/*.ts"
      - "apps/registry/public/docs/**"

jobs:
  check-docs-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate CLI docs
        run: node scripts/gen-cli-docs.mjs

      - name: Generate API docs
        run: node scripts/gen-api-docs.mjs

      - name: Generate llms.txt
        run: node scripts/gen-llms-txt.mjs

      - name: Format generated docs
        run: npx prettier --write apps/registry/public/docs/cli.md apps/registry/public/docs/api.md

      - name: Check for uncommitted changes
        run: |
          git diff --exit-code apps/registry/public/docs/cli.md || (echo "CLI docs are out of sync. Run: node scripts/gen-cli-docs.mjs" && exit 1)
          git diff --exit-code apps/registry/public/docs/api.md || (echo "API docs are out of sync. Run: node scripts/gen-api-docs.mjs" && exit 1)
          git diff --exit-code apps/registry/public/llms.txt || (echo "llms.txt is out of sync. Run: node scripts/gen-llms-txt.mjs" && exit 1)
          git diff --exit-code apps/registry/public/llms-full.txt || (echo "llms-full.txt is out of sync. Run: node scripts/gen-llms-txt.mjs" && exit 1)
          echo "All generated docs are in sync"
